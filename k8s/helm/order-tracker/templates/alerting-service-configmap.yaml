apiVersion: v1
kind: ConfigMap
metadata:
  name: alerting-service-config
data:
  SERVICE_NAME: "alerting-service"
  ENVIRONMENT: {{ .Values.environment | quote }}
  PORT: {{ .Values.alertingService.port | quote }}
