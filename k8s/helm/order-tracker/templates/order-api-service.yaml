apiVersion: v1
kind: Service
metadata:
  name: order-api
spec:
  selector:
    app: order-api
  ports:
    - port: {{ .Values.orderApi.port }}
      targetPort: {{ .Values.orderApi.port }}
  type: ClusterIP
