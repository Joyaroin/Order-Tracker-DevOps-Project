apiVersion: v1
kind: Service
metadata:
  name: alerting-service
spec:
  selector:
    app: alerting-service
  ports:
    - port: {{ .Values.alertingService.port }}
      targetPort: {{ .Values.alertingService.port }}
  type: ClusterIP
